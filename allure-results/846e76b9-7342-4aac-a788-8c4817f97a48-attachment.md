# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e\patient-listings.spec.js >> Patient Listings — @patients >> should load patient listings page successfully
- Location: tests\e2e\patient-listings.spec.js:39:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/patient/
Received string:  "https://portal.dev.metabolichealth-app.com/"
Timeout: 5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    13 × unexpected value "https://portal.dev.metabolichealth-app.com/"

```

```yaml
- img "Metabolic logo"
- paragraph: Sign in to manage patients
- text: Phone Number
- textbox "Phone Number":
  - /placeholder: Enter your phone number
- text: Password
- textbox "Password":
  - /placeholder: Enter your password
- link "Forget Password?":
  - /url: /forgot-password
- button "Sign In"
- heading "Don't have a password yet? Sign Up" [level=5]
- alert
```

# Test source

```ts
  1   | // ─────────────────────────────────────────────────────────────
  2   | // tests/e2e/patient-listings.spec.js
  3   | // Test suite for Patient Listings screen
  4   | // ─────────────────────────────────────────────────────────────
  5   | 
  6   | const { test, expect } = require('@playwright/test');
  7   | const { LoginPage }           = require('../../pages/LoginPage');
  8   | const { PatientListingsPage } = require('../../pages/PatientListingsPage');
  9   | 
  10  | // ── Shared login state ────────────────────────────────────────
  11  | // We log in once before all tests to save time
  12  | test.describe.configure({ mode: 'serial' });
  13  | 
  14  | test.describe('Patient Listings — @patients', () => {
  15  | 
  16  |   let loginPage;
  17  |   let listingsPage;
  18  | 
  19  |   // ── Login before every test ────────────────────────────────
  20  |   test.beforeEach(async ({ page }) => {
  21  |     loginPage    = new LoginPage(page);
  22  |     listingsPage = new PatientListingsPage(page);
  23  | 
  24  |     // Perform full login
  25  |     await loginPage.fullLogin(
  26  |       process.env.TEST_PHONE,
  27  |       process.env.TEST_PASSWORD,
  28  |       process.env.TEST_OTP
  29  |     );
  30  | 
  31  |     // Navigate to patient listings
  32  |     await listingsPage.goto();
  33  |   });
  34  | 
  35  |   // ══════════════════════════════════════════════════════════
  36  |   // 1. PAGE LOAD TESTS
  37  |   // ══════════════════════════════════════════════════════════
  38  | 
  39  |   test('should load patient listings page successfully', async ({ page }) => {
  40  |     // Assert URL is correct
> 41  |     await expect(page).toHaveURL(/\/patient/);
      |                        ^ Error: expect(page).toHaveURL(expected) failed
  42  | 
  43  |     // Assert page title is visible
  44  |     await expect(listingsPage.pageTitle).toBeVisible();
  45  | 
  46  |     // Assert patient count shows a number
  47  |     const count = await listingsPage.getPatientCount();
  48  |     expect(count).toBeGreaterThan(0);
  49  |   });
  50  | 
  51  |   test('should display all required table columns', async ({ page }) => {
  52  |     // All columns must be visible in the table header
  53  |     await expect(page.getByText('Patient')).toBeVisible();
  54  |     await expect(page.getByText('Diagnosis')).toBeVisible();
  55  |     await expect(page.getByText('TIR')).toBeVisible();
  56  |     await expect(page.getByText('Food Log')).toBeVisible();
  57  |     await expect(page.getByText('Weight')).toBeVisible();
  58  |     await expect(page.getByText('Active Chats')).toBeVisible();
  59  |     await expect(page.getByText('Status')).toBeVisible();
  60  |     await expect(page.getByText('Verification Date')).toBeVisible();
  61  |     await expect(page.getByText('Patient Program')).toBeVisible();
  62  |   });
  63  | 
  64  |   test('should display search bar and action buttons', async ({ page }) => {
  65  |     await expect(listingsPage.searchInput).toBeVisible();
  66  |     await expect(listingsPage.filterBtn).toBeVisible();
  67  |     await expect(listingsPage.addPatientBtn).toBeVisible();
  68  |     await expect(listingsPage.addPatientMRNBtn).toBeVisible();
  69  |   });
  70  | 
  71  |   test('should display saved filters row', async ({ page }) => {
  72  |     await expect(listingsPage.savedFiltersRow).toBeVisible();
  73  |   });
  74  | 
  75  |   // ══════════════════════════════════════════════════════════
  76  |   // 2. SEARCH TESTS
  77  |   // ══════════════════════════════════════════════════════════
  78  | 
  79  |   test('should search patient by name and show results', async ({ page }) => {
  80  |     const countBefore = await listingsPage.getPatientCount();
  81  | 
  82  |     // Search for a known patient name
  83  |     await listingsPage.searchPatient('Umer Mushtaq');
  84  | 
  85  |     // Patient count should decrease
  86  |     const countAfter = await listingsPage.getPatientCount();
  87  |     expect(countAfter).toBeLessThanOrEqual(countBefore);
  88  | 
  89  |     // Search results should contain the searched name
  90  |     await expect(page.getByText('Umer Mushtaq').first()).toBeVisible();
  91  | 
  92  |     // Clear Filter button should appear
  93  |     await expect(listingsPage.clearFilterBtn).toBeVisible();
  94  |   });
  95  | 
  96  |   test('should show correct patient count after search', async ({ page }) => {
  97  |     await listingsPage.searchPatient('Umer Mushtaq');
  98  |     const count = await listingsPage.getPatientCount();
  99  | 
  100 |     // Count in header should match visible rows
  101 |     const rows = page.locator('tbody tr');
  102 |     const rowCount = await rows.count();
  103 |     expect(rowCount).toBeLessThanOrEqual(count);
  104 |   });
  105 | 
  106 |   test('should clear search and restore full list', async ({ page }) => {
  107 |     const countBefore = await listingsPage.getPatientCount();
  108 | 
  109 |     // Search first
  110 |     await listingsPage.searchPatient('Umer Mushtaq');
  111 | 
  112 |     // Now clear
  113 |     await listingsPage.clearSearch();
  114 | 
  115 |     // Count should be back to original
  116 |     const countAfter = await listingsPage.getPatientCount();
  117 |     expect(countAfter).toBeGreaterThanOrEqual(countBefore);
  118 |   });
  119 | 
  120 |   test('should show no results for non-existent patient', async ({ page }) => {
  121 |     await listingsPage.searchPatient('ZZZZNONEXISTENTPATIENT99999');
  122 | 
  123 |     const count = await listingsPage.getPatientCount();
  124 |     expect(count).toBe(0);
  125 |   });
  126 | 
  127 |   test('should search is case-insensitive', async ({ page }) => {
  128 |     // Search uppercase
  129 |     await listingsPage.searchPatient('UMER MUSHTAQ');
  130 |     const countUpper = await listingsPage.getPatientCount();
  131 | 
  132 |     await listingsPage.clearSearch();
  133 | 
  134 |     // Search lowercase
  135 |     await listingsPage.searchPatient('umer mushtaq');
  136 |     const countLower = await listingsPage.getPatientCount();
  137 | 
  138 |     expect(countUpper).toBe(countLower);
  139 |   });
  140 | 
  141 |   test('should show clear filter button only when search is active', async ({ page }) => {
```